# P0演化图核对（10轮中间记录）

本图用于说明单次训练中不同尺度GT的平均正样本数量变化，不能用于断言STAL相对其他方法的增益。

数据来自已完成epoch 1–10的全部训练batch。没有舍弃已完成轮次或面积档；未来110轮不绘制。CSV保留四个阶段、三档的均值、P50/P90、零正样本比例和每轮样本量，输入JSON校验值见figure-manifest.json。

脚本预检查18项通过、无失败。两项提示的处理：本结题报告交付可编辑SVG/PDF及600dpi PNG预览，不以TIFF作为必要上传格式；当前只有一个训练run，GT观察并非独立seed，因此没有绘制虚假的跨seed置信区间。

PDF字体审计最小7pt，通过5pt下限。Python生成的整体预览已检查：图例不遮曲线，坐标文字完整，三档颜色与标记可区分，y轴从0起，无外推或平滑。图面标明10/120轮，不能作为完整120轮证据。

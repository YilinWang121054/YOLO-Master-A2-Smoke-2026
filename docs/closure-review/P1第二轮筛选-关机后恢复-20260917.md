# 第二轮筛选：关机后的恢复记录

9月17日，收到继续实验的确认后，我检查了剩余队列，并于北京时间14:48恢复最后一组 `s5-adaptive-compact-k8`。本次没有新增配置，也没有调整冻结的训练或筛选规则。

## 从哪里接着跑

前五组各20轮训练已经完成；完整训练产物的哈希复核通过。最后一组在9月16日03:29中断，日志停在第10轮的第1425/1618个batch。系统关机事件发生在03:29:24，训练进程于03:29:25退出，两者时间吻合。

`last.pt`保存到第9轮，SHA-256为 `2e59f733b1df3d86cbf70adf709232e7042da906cbd644365ba666ead6776490`。checkpoint中的配置、优化器、scaler以及模型/EMA/优化器浮点张量通过检查，CSV和在线统计也都对应9个完整epoch。因此从第10轮开头恢复，不改写CSV，不重跑前五组；未保存的第10轮批次需要重算。这里不承诺batch级或逐位一致的恢复。

14:49检查时，恢复后的日志已进入第10/20轮并持续推进。CPU已有23/30次完整评测；第五组的epoch19需要重新评测，epoch20及最后一组五次评测仍待完成。CPU监督器保持运行，但可用内存低于原定4 GiB门槛时会等待，不强制与GPU并发。

## 本次处理的两个问题

1. 原计划任务使用`pythonw.exe`，这次启动时Git校验子进程退出，尚未进入训练。同一冻结校验在当前会话中通过，随后使用`python.exe`、隐藏窗口和独立stdout/stderr日志成功启动。具体启动失败根因尚未确定，不将其归为模型故障。两项登录任务的动作已改为同样的带日志启动包装脚本，触发器、权限和任务设置不变。重新触发GPU任务测试时，OS锁正确识别已有监督器并退出0，没有创建第二个训练进程；尚未用再次重启电脑验证登录恢复。
2. 第五组epoch19的旧评测在关机时没有留下退出码。原始launch/stdout/stderr均保留，另附`interrupted.json`记录系统事件、哈希与“退出码未知”。归档工具现在允许这种有证据的历史中断，但仍要求五次完整评测及其哈希校验；不会把旧中断视为成功，也没有补造`exit_code=0`。相关测试共39项通过。

原始关机失败记录、随后计划任务的启动失败记录均另存保留。冻结的runner、evaluator、supervisor、selector和配置没有修改。变更仅涉及非冻结的启动包装、归档及其测试。

## 当前结论与后续

P0的原始基线材料和官方MATLAB实测对齐已经提交，仍以导师验收为准。P1尚未达标：这轮是同预算参数筛选，不是正式三seed、120轮提升结论。待剩余7次评测完成后，按事先冻结的规则统一选择；若没有合格候选，如实记录不入选，不事后放宽条件或追加第七组。

恢复证据见[检查记录与原始日志](../../results/p1-screen-v2-20260917-recovery/verification.json)。这份记录只说明恢复过程，不代替后续训练完成和评测结果。

记录性质：作者授权的实验运行记录；由本地进程、日志、系统事件及文件哈希核对整理，未将任何中断推断为成功。

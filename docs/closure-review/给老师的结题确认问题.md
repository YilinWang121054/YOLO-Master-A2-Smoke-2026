# 给老师的结题确认问题（未发送）

以下问题是在核对任务书和实际 checkpoint 后发现的新问题，不重复询问已经确认的面积定义、maxDets 或 120e 训练周期。建议先发前两段，它们直接影响剩余算力安排和结题判断。

老师您好，我的 A2 正在做结题核对。任务书 P0 要求“每 epoch 正样本统计”，已有的 120e 实验未完整采集这一项。我已准备使用公共锁定版本 acce839、v0.1-N、全量 VisDrone、batch=4、FP32、120e、MuSGD，从第一轮起记录全部训练 batch 的三档统计，并保留原实验。请确认：这次完整补跑及其分档评测，能否作为 P0 的主要验收包？最终材料冻结时间、答辩时间和登记表入口分别是什么，已提交 PR 但尚未合并是否影响通过？

当前冻结 adaptive 配置在前两对 seed 的 ΔAPs 为 −0.1625、−0.3405 个百分点，已不可能满足至少 2/3 seed 为正的条件。我会完整报告负结果，不临时改配置再挑最好的 seed。请问 P1 未达 +1.0 的情况下，完整 P0、三组对照、负结果机制分析，以及已被您合并的 TAL 冲突修复 PR #253，可以怎样认定结题与评优贡献？补充细则提到的高质量负结果，是否适用于 A2；还需达到什么分析深度？

核查版本与 optimizer 时还发现两处需要说明的偏差。现有 fixed 对照保留了原候选扩展，但统一加了 PR #253 的冲突修复；能否把它列为“修复后的 fixed 对照”，以新增的 acce839 补跑作为原始基线？另外 seed 20260824 的 fixed/adaptive 虽然 auto 日志显示 MuSGD(momentum=0.9)，warmup 后 checkpoint 的实际 momentum 为 0.937；seed 20260825/26 显式设置为 0.9。两组内部配对一致，但跨 seed 不一致。建议将第一对单列为探索性证据，正式矩阵缺项如实标注；是否必须补跑同协议第一对，或者允许在结题中报告这一偏差及局限？

总体评测早期使用了 `tjiiv-cprg/visdrone-det-toolkit-python`，但 CPU 对照发现它和 MATLAB 源码在正数 `.5` 取整、同分排序、ignore IoF 和边界处理上有差异。现在已按 MATLAB 源码新增 audit port，并对正式预测重算；本机 MATLAB 因 MathWorks 5201 无法运行原 devkit，当前还不能声称原工具数值严格对齐。是否有可运行官方 devkit 的公共机器，或指定的已验证 Python 实现可以使用？我们会提供同一份排序后的 DET TXT、版本和校验值，便于对齐。

最后，Mosaic 的最低补充预算请您明确一下：当前 adaptive 已没有稳定超过 fixed，按之前口径需考虑 fixed-off。临近结题，是优先把 pure TAL/adaptive/fixed 的 off 对照各做固定单 seed 120e，还是允许短周期只作机制补充？P2 的阈值、扩展幅度与 warmup 扫描及第二数据集/任务，是否需要全部完成才算 P2，还是按具体新增分析逐项认定？

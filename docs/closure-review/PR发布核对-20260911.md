# 最终A2 PR发布前核对

用户已授权核对完成后直接提交，再由用户审核修订。已于北京时间2026年9月11日01:53发布[PR #274](https://github.com/Tencent/YOLO-Master/pull/274)，早于9月12日截止，标题带 `[犀牛鸟-A2]`。发布结果已回读核验，证明见 `results/github-a2-final-pr-publication-20260911.json`；当前open，未合并。

## 代码状态

- 独立工作树：`YOLO-Master-A2-final-review`，分支 `review/a2-stal-final`。
- 最新上游：`7bfbfd374a6b44720f98366e088f3a962e16321d`。
- 已无冲突合并上游，当前提交：`6f558609882d9cc39379f88c912c9c3c5ec515d6`，工作树干净。
- 对最新main的差异仅5个文件：STAL测试、cfg校验、default.yaml、loss注入、tal实现。已合并的#253冲突修复不再出现在新增差异中。
- 训练源码仍保持52c2bef，P0原始基线仍为acce839；最终PR代码提交不冒充训练所用提交。

## 验证

CPU运行STAL、默认配置、模型配置、TAL冲突和MPS回归五个测试文件：33 passed、1 skipped。MPS测试被跳过；CPU结果不替代真实batch的CUDA AMP检查。日志与JUnit在 `logs/final-pr-upstream-tests-20260911.*`。

按新上游AGENTS.md执行全目录Ruff、format与codespell：全仓并未通过，诊断日志已保留，未顺手改动上游无关文件。A2涉及文件的15项Ruff诊断与最新main一致，新增0项；独立差异审计在 `results/closure-pr-lint-audit-20260911.json`。四个改动Python文件的format通过，五个改动文件的codespell通过。

最初误用 `python -m codespell` 报模块名不存在，随后使用环境内实际模块 `python -m codespell_lib` 完成检查，没有更改训练环境。全仓codespell退出65，改动文件检查退出0。

## 已完成的发布核对

1. 研究报告已纳入P0完整结果，79个表格数值逐项比对JSON通过；120轮曲线已完成源码与视觉检查。DET原MATLAB运行对齐仍未完成，不宣称官方一致性已验证或P0已被导师认可。
2. 12份原始训练/恢复日志、120份在线统计及548份DET预测包已发布。Git属性保留证据原始字节，136项清单/索引检查通过；日志不裁剪、不重编码。
3. 证据仓库双方历史已合并，正常非强制push成功。PR引用证据提交ce408692c0ce86c08f3803b2c4ceacedb6e941b6，公开报告SHA256与本地表格校验记录一致。
4. 功能分支Git传输失败后，通过已验证YilinWang121054身份的Git Data API发布2efc4d9。整个Git tree为f1ae8461c9108b89d41fee6df4fd95023ab031c1，与本地测试提交6f55860一致。API生成的合并后单提交与本地提交SHA不同，未冒称相同commit或改写训练历史。
5. 已创建PR #274，回读验证作者、完整标题、head、base及正文。没有执行merge；未完成的第三seed、Mosaic交互和P2实验明确列为未完成。接收用户审核后追加修订。

# 本地执行与审核安排

更新：2026-09-09。研究报告和 PR 在用户审核通过前不提交。

## GPU 队列（2026-09-10 已重新启动）

| 顺序 | 工作 | 原因与完成标志 |
| --- | --- | --- |
| 已暂停并保留 | adaptive seed 20260826 | checkpoint保留，暂停时约54轮；P0后从该点恢复 |
| 当前 | `p0-locked-s20260824-stats120` | 已启动，首轮已完成并写出 `assignment/epoch-001.json`；acce839原始基线、v0.1-N、全量6471/548、120e |
| 之后 | adaptive seed 20260826，接现有checkpoint | P0完成后恢复已有adaptive，不丢失已完成轮次 |
| 最后 | pure TAL seed 20260826，120e | 继续完成此前已授权的第三seed三组证据 |

P0 调到当前 adaptive 之前是为了执行“保底完成P0”的最新优先级。adaptive checkpoint 会保留并从断点恢复；不会修改模型、batch或训练周期。

执行脚本：`scripts/chain_p1_seed3.py`。P0 配置：`configs/p0-locked-s20260824-stats120.resume.json`。脚本新增操作系统持有的锁，重启后自动释放，避免重复 supervisor；遇到已有目录优先检查checkpoint，不能自动创建带后缀的新实验。

P0 恢复使用 `run_p0_baseline.py --resume <checkpoint>`，保持统计回调存在；每个完成epoch输出唯一的统计JSON。首epoch前中断可从同一YAML和seed重放首epoch。统计覆盖所有完成的训练batch，验证阶段不计入。

## CPU 并行工作

已完成：采集器真实烟测、恢复/统计/评测边界单测、六组历史预测面积重评、基线和optimizer核对、PR回归测试。下一步用CPU完成原devkit对齐排查、分档评测、统计曲线和报告核对。需要GPU的后续实验串行排期。

## 时间与材料

9月9日已经开始准备报告与PR，早于用户要求的最迟9月12日。正常单组120e约需23–25小时，P0还有统计开销，重启后的当前adaptive速度也应重新估计；不保证某个未经测量的精确完成时刻。

9月10–11日优先收齐P0在线统计与评测；9月12日安排用户审核和修订；任务书写9月13–14日冻结、答辩和交付，具体官方截止须老师确认。若算力或授权延迟，先报告P0风险，不能将缺项标记为完成。

## 审核文件

- `P0-P1-P2验收核对.md`：原文、指标、逐项缺口。
- `研究报告.md`：当前叙述稿，明确哪些实验和结论还没有完成。
- `PR说明.md`：四节PR正文，尚需最终结果及最新上游合并检查。
- `给老师的结题确认问题.md`：可直接转发，当前未发送。

代码审阅副本在同级 `YOLO-Master-A2-final-review`，分支 `review/a2-stal-final`。它与正在训练的 `YOLO-Master` 隔离。

## GitHub 与执行阻碍

GitHub API可读，两个仓库的Git HTTPS `ls-remote`也成功。远端证据main为738b77dc5db4baa2bbf493c24307f0b24cc585c9。PR253已合并。当前连接可用不代表所有写入都已测试通过。

本地证据分支在此前API同步后与远端历史分叉，需保留双方提交进行合并，不能force-push。此次自动审批拒绝了git fetch（理由：远端信任/修改引用需更明确授权），因此尚未合并。用户确认审核后再处理同步和发布。

自动审批曾拒绝启动计划任务（理由：启动持久监督任务可能触发长期GPU训练，需更明确授权）。用户于 2026-09-09 明确确认 P0 优先队列并授权 GPU/CPU 并行；2026-09-10 已停止旧 adaptive 进程、启动带文件锁的新监督器，并成功启动 P0。当前 P0 进程和监督器均在运行。

# 本地执行与审核安排

更新：2026-09-11。用户最新授权为：核对完成后先提交GitHub，用户随后审核，再按意见更新提交。此授权替代此前的发布前确认要求。最终[PR #274](https://github.com/Tencent/YOLO-Master/pull/274)和研究报告已公开，现等待用户后续审核意见。

## GPU 队列（2026-09-11 01:51核查）

| 顺序 | 工作 | 原因与完成标志 |
| --- | --- | --- |
| 已完成 | `p0-locked-s20260824-stats120` | acce839原始基线、v0.1-N、全量6471/548、120e；120份在线统计与原始日志已核验公开 |
| 当前 | adaptive seed 20260826 | 从54轮checkpoint恢复，目前62/120轮；训练参数未变 |
| 最后 | pure TAL seed 20260826，120e | 继续完成此前已授权的第三seed三组证据 |

P0此前调到adaptive之前以执行“P0优先”授权，现已完成。adaptive已恢复，不修改模型、batch或训练周期。

执行脚本：`scripts/chain_p1_seed3.py`。P0 配置：`configs/p0-locked-s20260824-stats120.resume.json`。脚本新增操作系统持有的锁，重启后自动释放，避免重复 supervisor；遇到已有目录优先检查checkpoint，不能自动创建带后缀的新实验。

P0 恢复使用 `run_p0_baseline.py --resume <checkpoint>`，保持统计回调存在；每个完成epoch输出唯一的统计JSON。首epoch前中断可从同一YAML和seed重放首epoch。统计覆盖所有完成的训练batch，验证阶段不计入。

## CPU 并行工作

已完成：采集器真实烟测、恢复/统计/评测边界单测、六组历史预测面积重评、基线和optimizer核对、PR回归测试、P0与fixed seed3完整评测、P0逐轮曲线及报告数值核对。CPU监督器等待下一组训练完成；原devkit运行对齐仍受MATLAB 5201阻碍。需要GPU的后续实验串行排期。

## 时间与材料

9月9日已经开始准备报告与PR，早于用户要求的最迟9月12日。正常单组120e约需23–25小时，P0还有统计开销，重启后的当前adaptive速度也应重新估计；不保证某个未经测量的精确完成时刻。

用户9月10日明确要求：在9月12日前完成PR提交和标题修改，标题包含 `[犀牛鸟-A2]`。最终PR已于北京时间9月11日01:53提交，标题和目标分支已回读确认；用户按最新授权在发布后审核。任务书原定9月13–14日的冻结、答辩安排不能替代此次PR截止要求。

9月10–11日优先收齐P0在线统计与评测，同时完成最终PR代码差异核对和报告审阅包。若后续P1/P2实验仍未结束，稿件明确列出未完成项，不等待它们全部结束才提交，也不预写达标结论。按最新授权，核对完成即可发布PR/报告，然后提供链接让用户审核；有修改意见后追加提交，不覆盖历史证据。

9月10日晚已完成PR #235和#253标题修改，并使用YilinWang121054身份回读验证。最终功能PR #274标题为 `[犀牛鸟-A2]：Add configurable scale-aware TAL assignment for small objects`。独立审阅工作树已合并upstream main `7bfbfd374a6b44720f98366e088f3a962e16321d`，五文件差异与回归已核对：33 passed、1 skipped，训练源分支不变。

## 审核文件

- `P0-P1-P2验收核对.md`：原文、指标、逐项缺口。
- `研究报告.md`：当前叙述稿，明确哪些实验和结论还没有完成。
- `PR说明.md`：已发布PR的四节正文模板，实际在线正文的证据链接已绑定固定提交。
- `给老师的结题确认问题.md`：可直接转发，当前未发送。

代码审阅副本在同级 `YOLO-Master-A2-final-review`，分支 `review/a2-stal-final`。它与正在训练的 `YOLO-Master` 隔离。

## GitHub 与执行阻碍

GitHub证据仓库的正常HTTPS push本次成功，ce40869已公开且报告字节经API回读核验。功能分支Git传输仍出现间歇中断，已通过Git Data API发布相同完整代码树并创建PR #274。PR253已合并；本次最终PR尚未合并。

本地证据分支与远端分叉已保留双方提交完成合并，唯一脚本冲突保留当前恢复及电池选项，没有force-push。此前fetch和计划任务遇到的审批阻碍是历史状态，不是当前仍未提交的原因。

用户已明确确认P0优先队列并授权GPU/CPU并行；当前GPU在执行adaptive seed3，CPU等待其完成后评测。监督器保留文件锁和checkpoint校验，登录恢复任务仍启用。P0已结束，不应再重复启动。

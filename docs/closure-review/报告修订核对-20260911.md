# 9月11日报告修订核对

本轮只更新已经确认的协议归类和新增评测证据，没有重写研究问题或机制解释。

| 固定用语 | 本文含义 | 本轮处理 |
| --- | --- | --- |
| 原始基线 | acce839的P0补跑 | 不与含#253的fixed混称 |
| 修复后的fixed对照 | 52c2bef训练中的fixed模式 | 与原始基线区分 |
| MATLAB-source audit port | 按官方源码移植的Python总体评测 | 不改称MATLAB实测结果 |
| APs | 原图面积<1024、IoU .50:.95、maxDets500 | 差值用绝对百分点，不改成AP50s |
| 探索性实验 | seed20260824协议有差异的组 | 排除正式三seed对比 |

修订记录：

- 将“待导师确认是否单列seed20260824”替换为已确认的排除规则，早期缺逐轮统计的组明确仅供参考。
- 保留所有结果表和负结果；删除把探索性seed计入正式“2/3正向”判定的推断，改为正式矩阵仍不完整。
- 方法中简述源码核验和Python复算，把逐项源码对应、29项边界测试、输入域等细节放在独立审计说明，不把整份review材料堆进研究报告。
- 报告17行表格（含表头/分隔线）与修订前逐行相同；已有79个数值单元格的核验不因本次编辑而改变。图、源数据及其caption未改动。

主文检查：内容仍是“覆盖是否增加→精度未提升→可能机制与限制”的原有证据链，未加入显著性、因果或达标声明。数值主报告仍在原表格，复算细节在附属说明。按统一LF且不计文件结尾换行统计，正文字符由8256变为8496，增加240字符；这不是英文word count。

本次使用academic-research-suite检查可复现边界，并用nature-polishing检查方法、结果的措辞。类型为算法实验报告，中文保留中文，未套用期刊宣传性写法；最终科学判断和结题认可由作者与reviewer负责。

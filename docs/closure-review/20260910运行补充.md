# 9月10日运行补充（本地待审核）

P0优先队列已启用，不再等待启动授权。GPU先跑acce839原始基线120轮及逐epoch在线统计，再恢复adaptive seed20260826的54轮checkpoint，最后运行pure TAL seed3。已完成轮次的统计与checkpoint均独立核查；P0尚未完成，不能标记验收通过。

CPU现在有独立的评测队列，登录任务为 `YOLO-Master-A2-CPU-Evaluation`。它与GPU队列分开，按完成的checkpoint运行完整548张验证、DET源码移植评测与COCO-style面积补充，并保存输入/配置/源码校验值。进程通过文件锁防重复；某轮评测失败后停止并留下日志。

fixed seed20260826的last.pt已完成CPU评测，FP32、batch4、imgsz800、conf0.001、IoU NMS0.7、maxDets500。结果仅供本地审阅：

| DET AP* | AP50* | AP75* | AR500* | APs | APm | APl | ARs@500 |
| ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| 21.7828 | 39.6533 | 20.6035 | 38.2678 | 12.6454 | 30.8524 | 40.8172 | 28.0815 |

*DET列由MATLAB源码的Python审计实现计算；未取得原MATLAB运行对齐证明。结果、预测校验值和CPU设备记录在 `results/closure-evaluation/p1-fixed-s20260826/`。与先前GPU推理结果合并作多seed结论时，应明确推理设备，不把CPU/GPU算子数值等价视为未经验证的前提。

已将COCO补充评测中的ignore取整改为MATLAB正数半值向上，并修正图像末行/末列的边界像素；其独立边界样例已通过。六组旧预测已用新实现重评，保存在 `results/area-audit-20260910/`，9月9日结果保留。adaptive−fixed的前两对ΔAPs为−0.1625058和−0.3405016，描述性平均−0.2515037个百分点，原负结果判断不变。

P0已生成10轮中间正样本演化图和完整源数据CSV，路径 `results/p0-interim-20260910/figures/`。这不是120轮最终图。P0训练完成后，CPU评测队列会检查120份统计再导出完整曲线。报告、PR和所有新图此时仍未上传；9月10日晚用户已授权核查完成后直接发布，再审核修订。

## 11时运行检查

CPU评测监督器的状态在06:26后停止更新。检查时进程已不存在，计划任务上次退出码为 `0x8007042B`。Windows电源事件显示06:26:46切到电池，06:27:51恢复交流电；任务恰好设置了“转电池时停止”。这些证据高度支持电源策略导致退出，但缺少TaskScheduler直接终止事件，不能断言已取得完整因果日志。

11时已重新启动CPU评测任务，状态恢复为等待训练完成。训练与CPU评测两个监督任务都取消了转电池自动终止设置，原有登录触发、进程互斥和7天运行上限保留。本次没有重启或修改正在运行的P0训练；GPU仍在训练，CPU评测任务等待完整checkpoint。系统关机/休眠仍会暂停计算，恢复后继续按已有checkpoint处理。

## 晚间核查、恢复与PR标题

Windows记录最近一次启动为22:12:13，P0原日志停在第120轮605/1618 batch；CSV与last.pt均为完整119轮。登录后的GPU监督任务退出码为1，CPU监督任务正常启动；缺少当次stderr，暂不归因于某个具体代码异常。

23:04的恢复预检查确认：checkpoint零基epoch=118、optimizer/scaler存在，代码仍为acce839，119份训练统计覆盖完整，无需回退CSV。23:06使用带独立stdout/stderr的新监督进程恢复同一run，GPU训练PID24368、监督PID36484；这些PID仅为当次记录。恢复日志位于 `recovery-logs/p0-locked-s20260824-stats120/resume-20260910-230610.*.log`。最后一轮重跑是epoch级恢复，不声称恢复到中断batch。

GitHub连接器仍返回403，但本机凭据确认为YilinWang121054，可访问GitHub官方API并完成标题写入。PR #235和#253均加上 `[犀牛鸟-A2]：` 前缀；回读确认正文、head/base SHA、状态和合并时间均未变。两条均为此前已合并的修复，不等同于最终STAL功能PR。

用户要求9月12日前提交PR并修改标题，内部截止为北京时间9月11日内。随后用户明确授权先提交GitHub、再审核修订，旧的发布前确认要求不再适用。最终STAL功能PR和报告仍需更新实际结果、完成最新上游适配与测试后发布。

# 候选诊断图与报告修订核对

本轮为算法课题报告的结果／讨论局部更新，保留中文，不套用期刊格式或翻译全文。先固定图的选例与纳入规则，再把已完成的局部诊断替换进报告；研究问题、验收口径、训练参数和原始记录均不变。

## 术语与证据位置

| 术语 | 本文固定含义 | 防止混淆 |
| --- | --- | --- |
| P0原始基线 | acce839的120轮基线 | 不是含#253修复的fixed |
| fixed／adaptive／pure TAL | 已有三种分配模式 | 不新增算法名称 |
| APs | 原图small、IoU=.50:.95、maxDets=500 | 不是探针resize后分档，也不是AP50s |
| 正样本数 | 冲突后mask计数 | 不等于target score或其和 |
| target score／权重和 | 归一化监督权重／每GT的权重之和 | 候选均值与GT均值不互换 |
| 普通IoU／clamped CIoU | 图中定位质量／实际排序指标 | 分别记录，不替换算法定义 |

| 材料 | 功能与处理 |
| --- | --- |
| 数量与监督权重增幅不一致 | 新的局部观察，放正文；保留零权重比例的不利变化 |
| 配对图、质量分布、空间示例 | 图中展示，完整定义和CSV放图版说明；不在正文重列全部分档表 |
| 同checkpoint、无随机增强、非训练补录 | 会改变解释范围，正文保留 |
| 原始候选、48次等价检查、逐行核验 | 用公开证据链接承载细节，不复制运行日志 |
| fixed checkpoint限制、无Mosaic对照 | 正文保留，不写成已证明的长训失败原因 |

用实际诊断替换过时的“探针即使补做”段落及两段纯计划描述。早期smoke观察仍保留作区别，P0真实训练期记录不与离线探针合并。标题不重复完整结论，图注负责读图，正文负责观察与边界；没有新增p值、CI或跨协议seed均值。

机制小节由477增至1428字符（含中文、ASCII与Markdown），空白分隔单元32→41，不称为中文词数；整篇由8866增至9851字符。增加来自已完成诊断、两张图及必要图注，不是给未做消融增添解释。九行对照表、P0结果和统计保持原值。

一致性扫描仍只提示整数1与+1.0的精度差，分别为配置/计数与验收阈值，保留。95个表格数值和所有相对链接再次核验通过；新增段落12个数值表达匹配诊断记录，1321条配对及8404条候选CSV逐行相符。图版核验同时保留16项测试结果。见[报告表格核验](../../results/report-table-verification-20260913-figures.json)及[图版核验](../../results/candidate-quality-20260913/figures-v1/verification.json)。这些检查不代表导师已通过验收或作者已完成阅读审核。

本次nature-figure约束了全样本配对展示、预选空间例子和逐图QA；nature-polishing用于替换过时计划、明确数量/权重区别及限制因果措辞。没有改写原始实验数值或提升结论等级。

# 第二轮 fixed 对照已启动

北京时间 2026-09-13 21:44 启动 `p1-v2-fixed-s20260825-stats120`。这是新增的协议补齐实验，不覆盖原 fixed seed20260825 结果。

- GPU：RTX 3060 Laptop；全 train 6471 / val 548，imgsz=800，batch=4，nbs=64，FP32，120 epoch，patience=0。
- 模型从 YAML 初始化，pretrained=False；显式 MuSGD、lr0=.01、momentum=.9、weight_decay=.0005、warmup=3/.8/0；Mosaic on，close_mosaic=10。
- 模型源码保持 `52c2befa50706b9dff13b6e0813b19413d9f532d`。完整配置：[train.json](../../configs/p1-v2-fixed-s20260825.train.json)。runner 和恢复配置在首次启动前提交于 41c28f3，启动器的进程识别修正于 7d08a0d。
- 原始日志：`F:/YOLO-Master-A2-P1/recovery-logs/p1-v2-fixed-s20260825-stats120/initial.stdout.log`、`initial.stderr.log`。启动 manifest、完整解析后的默认参数、配置/模型/runner/observer 哈希在同目录的 JSON 中。
- 输出：`F:/YOLO-Master-A2-P1/p1-v2-fixed-s20260825-stats120`。逐轮真实增强统计位于 `assignment/epoch-NNN.json`；每轮完整结束后才能将该轮记作完成。当前刚开始，不写成已获得120份记录。
- 登录恢复任务 `YOLO-Master-A2-P1-V2-Fixed-Resume` 已注册。恢复前检查 source commit、关键参数、optimizer/scaler、CSV 和在线统计；保留 checkpoint 回退与 CSV 备份。21:48 只读检查识别到正在运行的训练进程和有效初始 last_healthy.pt，没有重复启动。

恢复命令（可先加 `--check-only`）：

```powershell
& 'F:/conda-envs/yolo-master/python.exe' -X utf8 scripts/resume_p1_training.py --config configs/p1-v2-fixed-s20260825.resume.json
```

中断发生在一轮内部时，从最近完整 checkpoint 续训并重跑未完成部分；不承诺从任意 batch 逐位接续。原始日志不覆盖，异常不自动改 batch 或换 seed。第一轮尚未完成时，初始 checkpoint 用于核验并重放第一轮。

新的 runner 已在 CPU mini dataset 上完成1 epoch及在线统计检查；该 smoke 不是 VisDrone 正式结果。六项恢复控制器测试通过，P0 observer 与 P1 probe 两套测试分进程运行分别4项和10项通过。旧三模式队列已经结束，不重启旧任务。

后续仍须补齐 adaptive 有限筛选、统一协议三 seed 主对照及 Mosaic 交互，不能只凭这个 fixed 实验认定 P1 完成。正式验收阈值、缺项和需老师确认的问题见[第二轮方案](P1第二轮补齐方案-20260913.md)。

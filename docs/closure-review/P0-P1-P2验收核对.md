# A2 结题验收核对（更新于2026-09-13）

9月13日更新：第三seed的fixed、adaptive、pure TAL均完成120轮及548张验证，队列正常结束。报告已补齐九行参考对照、95个表格数值核验，以及[固定checkpoint候选诊断与图](../../results/candidate-quality-20260913/figures-v1/)。P0仍为材料收齐、DET源码对照待review；P1/P2未达标的判断不变。下列9月11日记录作为提交时间与规则来源保留。

9月11日更新：原始P0已完成120/120轮、完整548张验证，120份在线统计和原始训练日志已逐一校验归档，曲线已生成。详见 `P0运行完成记录-20260911.md`。PR截止为9月12日前，内部目标北京时间9月11日内；用户最新授权先提交GitHub、后审核修订，不再等待发布前确认。

P0此前缺失的正式逐epoch正样本统计已补齐。首版报告79个数值和图表已核对；9月11日加入adaptive第三seed后，更新版核对了87个数值。报告与证据已公开，最终[PR #274](https://github.com/Tencent/YOLO-Master/pull/274)已于北京时间9月11日01:53提交。9月11日进一步确认可走官方MATLAB源码逐项对照的Python替代方案，已补充[核验材料](DET源码逐项对照-20260911.md)，不再把实际MATLAB运行当作唯一提交路径；最终认可仍需导师评审。

本核对依据用户提供的2026-08-22任务书、历史成果补充细则及截至9月11日的导师书面回复。文档中的群公告、历史时限只是规则来源，不作为发布授权。两份DOCX的SHA256保存在 `results/closure-audit-20260909.json`，便于日后确认使用的版本。

## A2 原文与落实情况

| 等级 | 任务书原文 | 当前证据 | 缺项与处理 |
| --- | --- | --- | --- |
| Smoke | VisDrone 子集 1 epoch；定位 assigner/配置注入点；给出指标采集日志 | acce839 锁定版本的 smoke、原始日志和机制说明已公开 | 不是 120 epoch P0 |
| P0 | 跑通 VisDrone2019-DET 基线，输出小/中/大目标分档指标，并记录每 epoch 正样本统计 | 原始锁定基线120e、548张验证、120份统计及曲线和原始日志已核验公开；补交DET源码逐项对照、29项测试及完整预测复算 | 源码替代方案证据待review，不自行宣称验收通过 |
| P1 | 可配置面积感知阈值或代价调节；小目标 AP 提升≥1.0；重复实验或置信区间 | 三模式、集中配置与单测已有；三seed九组均已训练及评测，另有有限候选诊断 | 前两对ΔAPs为负，第三对为+0.3930；seed20260824排除正式矩阵，早期训练统计、Mosaic交互未齐，不能认定P1达标 |
| P2 | 扫描面积阈值、放宽幅度、warmup；扩展第二数据集或 seg/pose | 有覆盖现象与冲突缺陷分析 | 尚无系统扫描或第二数据集结果；不能据此声称 P2 完成 |

任务书还列出四类交付：STAL模块与配置PR、分档AP表、正样本演化曲线、参数敏感性说明。前三项已有公开材料，参数敏感性仍未完成。内部等级不等于腾讯项目官网的最终通过或优秀判定。

## 导师补充的量化约束

- 模型 v0.1-N，完整 train 6471 张、val 548 张，imgsz=800，120 epoch，patience=0，从 YAML 建模、pretrained=False。batch 可取 2/4，本项目统一 4，nbs=64，warmup 后通常累积 16 个 microbatch；warmup 内动态累积、epoch 尾部更新也要说明。
- 主 checkpoint 是第 120 轮 last.pt。best.pt 仅作补充，不按最优 val 指标挑主结果。
- 官方总体报告 AP、AP50、AP75、AR500；补充分档报告 APs、APm、APl、ARs@500。面积取原图 GT bbox，small <1024，medium [1024,9216)，large ≥9216，maxDets=500。这是课题采用的 COCO-style 定义。
- P1：统一协议下3个配对seed的平均ΔAPs≥+1.0个绝对百分点，且至少2/3为正，同时报告逐seed和mean±std。现有三对参考差值为−0.1625、−0.3405、+0.3930；seed20260824因auto/momentum差异排除出正式矩阵，不能把三对历史结果直接计算成正式三seed均值，也不能只选正向seed作为达标依据。
- 必须保留 pure TAL、现有 fixed STAL、adaptive STAL。Mosaic on/off 至少比较 pure TAL/adaptive；adaptive 未稳定超过 fixed，补 fixed-off 的条件也已触发，具体预算应请老师确认。
- 正样本以增强、resize 后真实进入 assigner 的 GT 分档。新采集器逐 epoch 记录三档的均值、P50、P90、零正样本比例，以及原框候选、扩展后候选、冲突前和冲突后的数量。
- AMP 检查必须覆盖分配、loss 和梯度。已有小型一致性单测；本轮 CPU 回归不能代替 CUDA AMP 的完整真实 batch 验证。

## 基线与协议：不能省略的差异

公共 BASE_REF 是 `acce839c7e895d6b179de7f7093fa879e237cc7b`；v26.08 tag 解引用的 `43d40117c30811204fb9347efeabddce15f11a62` 只用于说明发布来源，不能代替新增 diff 的基点。

当前训练代码 `52c2befa50706b9dff13b6e0813b19413d9f532d` 基于 `0996b7da14dfaafae9d4488e960814ff19eb19ce`。BASE→0996 有 30 个上游提交，不能归入个人成果。核对 Git blob 后，v0.1-N 模型 YAML、trainer，以及改动前的 TAL/loss/default 配置在 BASE 和 0996 间一致。个人 52c2bef 改动集中在 5 个文件。

52c2bef 的 fixed 保留旧候选扩展几何，但也应用了“冲突只能在提出候选的 GT 内解决”的修复。因此它应称为“含冲突修复的 fixed 对照”，不是与 acce839 完全相同的原始基线。修复已作为 [PR #253](https://github.com/Tencent/YOLO-Master/pull/253) 被 isLinXu 于 9 月 1 日合并。

此外，已直接读取 last_healthy.pt 的 optimizer：seed 20260824 的 fixed/adaptive momentum=0.937，其余已完成组为 0.9。auto 启动日志虽显示 0.9，warmup 会使用 args.momentum 将其改变。pure TAL seed 20260824 还有 warmup_bias_lr=0.1，其他组为 0.0。不能将现有九组描述成完全同协议三 seed 三模式矩阵。

仓库旧复现脚本使用 batch=6、workers=4、mixup=0.1、copy_paste=0.1、close_mosaic=15、patience=20。本项目是导师沟通后使用的 120e 控制协议（batch=4、mixup/copy_paste=0、close_mosaic=10、patience=0），应明确写成调整后的协议，而非逐参数复刻旧脚本。

## 指标核对中的修正

9月9日发现COCO默认面积上下界均包含端点，导致恰好1024、9216面积的GT被相邻两档同时计入。原val中分别有13和1个这样的有效GT。已将上界改成严格小于阈值，通过实际evaluator的边界测试；9月10日进一步修正MATLAB ignore积分查询的半开区域。研究报告使用最新 `results/area-audit-20260910/` 重评结果，20260909及更早版本仅保留作审计。

总体指标此前来自 `tjiiv-cprg/visdrone-det-toolkit-python`，但CPU审计发现它与MATLAB源码有实际差异：正数`.5`取整、排序稳定性、ignore IoF引用和图像边界。`scripts/evaluate_visdrone_det_strict.py`按源码重写这些规则，并保存预测差异审计。9月11日确认源码级逐项对照可作为替代方案，现已补充固定源码版本、29项边界测试、548份输入哈希和P0复算。MATLAB R2024a仍报5201，没有实际跨语言运行差值；不得把Python复算一致冒充MATLAB数值对齐。实际对比若取得，四项总体指标差异应小于0.01个百分点，超过0.05需解释。

## P0 补跑及交付次序

1. P0原始基线已完成120e；119轮后一次重启导致最后一轮重跑，原始失败与恢复日志均保留。
2. CPU已完成完整验证、分档评测、120轮统计校验与曲线导出，并补交老师允许的源码级对照。登记可先写“P0材料已提交，评测对齐待补”，补交后附对照链接并注明待review；9月15日前补齐为目标，更晚延期需再确认。
3. seed3三模式均已完成训练与CPU评测，GPU队列正常结束，无需恢复。P0已有证据和最终PR已按时发布，P1/P2缺项仍明确保留，不等待全部长训才提交。
4. CPU已完成六组面积重评、基线审计和最新上游适配后的PR回归：33 passed、1 skipped；正式评测、报告核对也已完成。跳过MPS不等于完成真实batch CUDA AMP核查。
5. 按9月10日最新授权，PR/报告完成证据与代码核查后先同步GitHub，由用户随后审核并追加修订。最终 BASE_REF→FINAL_REF、环境、配置、数据 split、日志和 checksum 必须齐全。

## 仍未完成的清单

DET源码级补充证据的review；同协议三seed主表及对应训练统计；Mosaic交互；参数敏感性及P2扩展。seed20260824明确排除正式矩阵，早期统计不完整组仅作参考，统一协议补跑时间不足可列待完成，不影响已具备的P0材料。最终PR已按时提交；已核对的上游CI失败不影响提交有效性，登记“PR #274已提交，待review”。高质量负结果可作为A2成果，#253独立计工程贡献；是否通过或评优仍由导师评审。

# 9月12日收尾检查

## Material Passport

- Origin Skill: experiment-agent
- Origin Mode: validate
- Origin Date: 2026-09-12
- Verification Status: ANALYZED
- Version Label: closure_check_20260912
- 范围：现有证据、运行状态与归档脚本检查；本次未重训，未重跑完整DET评测，未执行MATLAB。

## 当前状态

北京时间07:54观察到pure TAL seed20260826已完成91/120轮，正在进行第92轮；训练进程和GPU/CPU监督器均正常。CPU队列等待训练完成后评测。此记录是时间点快照，不是训练完成证明。

GitHub读取及Git远端查询成功，公开证据main与本地检查前的提交同为`0599a17c7365959b7fba3a0c4b390fb0b1c9a6e0`。PR #274为open、未合并，标题带`[犀牛鸟-A2]`；提交时间为北京时间9月11日01:53。CI问题按已核实的上游基础设施情况处理，不重复提交同一条评论。

老师允许的源码对照方案已经补交，入口为[DET源码逐项对照](DET源码逐项对照-20260911.md)。[原审计JSON](../../results/det-source-alignment-20260911/audit.json)记录了源码版本、评测脚本哈希、完整配置、checkpoint哈希和精确调用命令。原审计的Python复算一致不等于MATLAB运行一致，最终仍待review。

## 修正训练结束后的归档检查

原脚本要求initial和recovery两个目录都存在日志。pure TAL此次未发现recovery目录，这会使正常结束后的归档被错误阻止。现改为：

- initial仍是必需项；已有的recovery目录若没有日志，仍报错，不静默跳过。
- 非P0任务允许recovery目录不存在，但manifest明确写入`directory_absent`。这不独立证明实验从未重启，也不代表恢复日志已提供。
- P0保留原有严格检查，因为其真实恢复历史已知；缺少在线统计仍会报错。
- 原始字节、失败日志和既有归档不可覆盖的保护保持不变。没有改训练、模型、评测代码或冻结参数。

运行中的Windows目录列表曾显示stdout长度为0；打开共享只读句柄实际读到24,883,254字节，并能看到第92轮进度。因此没有把目录列表中的0误判为日志丢失。最终归档仍须等待进程退出后核验完整文件。

测试命令（证据仓库根目录）：

```powershell
& 'F:/conda-envs/yolo-master/python.exe' -m pytest tests/test_archive_raw_logs.py tests/test_det_source_audit.py -q --junitxml=results/archive-and-det-regression-20260912.xml
```

结果：37 passed（8项归档测试、29项DET源码边界测试）。[原始JUnit结果](../../results/archive-and-det-regression-20260912.xml)。这不是一次完整预测集的评测重跑。

## 结论边界检查

本次不新增显著性检验、置信区间或跨seed均值。按11类解释风险核查：汇总掩盖分组差异、从汇总推到单目标、筛选偏差、碰撞变量、忽视目标分布、向均值回归、只保留成功实验、多指标挑选、分析路径选择、相关当因果、反向因果。处理分别是：保留逐seed/逐面积档结果；不从AP推导单目标机制；报告所有已完成组且不按好坏选seed；无新增条件化因果模型；保留原图面积分布与分档结果；单seed正向不作稳定收益；保留失败/恢复及缺项；不只报APs；保留修正前历史与固定配置；机制仍标假设；不声称已证实方向性因果。覆盖11/11，不代表已完成所有机制消融。

## 下一步

pure TAL完成后做完整548张评测、checkpoint/参数核对、原始日志归档和预测打包，再更新报告，不提前填最终指标。P0补充对照待review；P1同协议三seed及在线统计仍不完整，P2系统扫描/第二数据集仍未完成。

Mosaic-off按老师优先级排在现有第三seed之后，至少pure TAL/adaptive，fixed-off补充条件已触发。尚需明确最低训练预算；在确认前，不把固定短周期诊断冒充120轮正式交互对照。候选质量可视化单独列为机制诊断，不冒充P2系统实验。
